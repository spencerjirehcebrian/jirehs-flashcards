# Test Environment Configuration
# Copy to .env.test and fill in your values
#
# IMPORTANT: Use a SEPARATE database and bucket from development
# to avoid data conflicts during test runs.

# Database (separate test database)
DATABASE_URL=postgres://user:password@152.53.136.253:5446/flashcards_test

# S3/R2 Storage (separate test bucket)
S3_BUCKET=flashcards-test
S3_REGION=auto
S3_ENDPOINT=https://xxx.r2.cloudflarestorage.com
S3_ACCESS_KEY=your_access_key
S3_SECRET_KEY=your_secret_key

# Server (different port to avoid conflicts)
HOST=0.0.0.0
PORT=3001

# Logging
RUST_LOG=info
